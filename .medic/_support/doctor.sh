#! /usr/bin/env bash

#DIR=$(dirname $0);
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
source "$DIR/os.sh"
source "$DIR/cecho.sh"
source "$DIR/check.sh"
source "$DIR/step.sh"
