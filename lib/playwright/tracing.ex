defmodule Playwright.Tracing do
  @moduledoc """
  ...
  """
  use Playwright.SDK.ChannelOwner

  # @spec start(Tracing.t(), options()) :: :ok
  # def start(tracing, options \\ %{})

  # @spec start_chunk(Tracing.t(), options()) :: :ok
  # def start_chunk(tracing, options \\ %{})

  # @spec stop(Tracing.t(), options()) :: :ok
  # def stop(tracing, options \\ %{})

  # @spec stop_chunck(Tracing.t(), options()) :: :ok
  # def stop_chunck(tracing, options \\ %{})
end
