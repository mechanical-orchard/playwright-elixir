# defmodule Playwright.Network.RouteTest do
#   use Playwright.TestCase, async: true

#   alias Playwright.{Page, Request, Response, Route}

#   describe "Route.fulfill/3" do
#     test "fulfills a route's request with the given response.", %{assets: assets, page: page} do
#     end
#   end
# end

# async def test_request_fulfill(page, server):
# async def test_request_continue(page, server):
