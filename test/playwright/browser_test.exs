defmodule Playwright.BrowserTest do
  use ExUnit.Case

  alias Playwright.Browser

  # describe "connect/2" do
  #   test "when ... some error ... does something" do
  #     {:ok, pid} = Browser.start_link()

  #     assert {:error, "FAIL!"} ==
  #              Browser.connect(pid, ws_endpoint: "ws://localhost:3000/playwright")
  #   end
  # end

  # describe "new_page/0" do
  #   test "creates a new page" do
  #     assert {:ok, page} = Browser.new_page()
  #   end
  # end
end
